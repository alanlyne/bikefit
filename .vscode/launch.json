{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Run with Python Debugger",
        "type": "debugpy",
        "request": "launch",
        "program": "${workspaceFolder}/bikefit/main.py",
        "console": "integratedTerminal"
        // "args": "${command:pickArgs}"
      }
    ]
  }
